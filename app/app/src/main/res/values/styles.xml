<resources>

    <!-- Base application theme. -->
    <style name="WatchMyBackTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Color -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryLight</item>
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorSecondaryVariant">@color/colorAccentDark</item>
        <item name="colorError">@color/colorAccentDark</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorOnSecondary">@color/colorOnAccent</item>
        <item name="colorOnError">@color/colorOnAccent</item>
        <item name="textInputStyle">@style/TextInputLayoutWatchMyBack</item>
        <item name="materialButtonStyle">@style/ButtonWatchMyBack</item>

        <!-- Text -->
        <item name="fontFamily">@font/roboto</item>
        <item name="android:fontFamily">@font/roboto</item>

        <!-- component default style -->
        <item name="shapeAppearanceLargeComponent">@style/ShapeAppearance.WatchMyBackTheme.LargeComponent</item>
        <item name="shapeAppearanceMediumComponent">@style/ShapeAppearance.WatchMyBackTheme.MediumComponent</item>
        <item name="shapeAppearanceSmallComponent">@style/ShapeAppearance.WatchMyBackTheme.SmallComponent</item>
    </style>

    <!-- TOOLBAR THEME -->
    <style name="ToolBarTheme" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/colorOnPrimary</item>
    </style>
    <!-- TOOLBAR TITLE THEME -->
    <style name="ToolBarTheme.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">@dimen/toolbar_text_size</item>
    </style>

    <!-- Shapes -->
    <style name="ShapeAppearance.WatchMyBackTheme.LargeComponent" parent="ShapeAppearance.MaterialComponents.LargeComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">6dp</item>
    </style>

    <style name="ShapeAppearance.WatchMyBackTheme.MediumComponent" parent="ShapeAppearance.MaterialComponents.MediumComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">4dp</item>
    </style>

    <style name="ShapeAppearance.WatchMyBackTheme.SmallComponent" parent="ShapeAppearance.MaterialComponents.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">20dp</item>
    </style>

    <!-- Button -->
    <style name="WatchMyBackFloatingButton" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="elevation">@dimen/normal_elevation</item>
    </style>

    <style name="ButtonWatchMyBack" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="strokeWidth">2dp</item>
        <item name="strokeColor">?attr/colorSecondary</item>
        <item name="android:textColor">?attr/colorSecondary</item>
        <item name="iconTint">?attr/colorSecondary</item>
        <item name="rippleColor">?attr/colorOnSecondary</item>
    </style>

    <!-- Text -->
    <style name="TitleWatchMyBack">
        <item name="android:textSize">@dimen/text_title</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="TextInputLayoutWatchMyBack" parent="Widget.MaterialComponents.TextInputLayout.FilledBox">
        <item name="boxBackgroundMode">filled</item>
        <item name="boxBackgroundColor">@android:color/transparent</item>
        <item name="hintTextColor">@color/colorOnPrimaryAlpha</item>
    </style>


</resources>
